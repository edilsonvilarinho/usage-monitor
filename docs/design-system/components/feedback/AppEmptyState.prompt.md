Nothing in this slice — a legitimate result, not a failure.

```jsx
<AppEmptyState message="Nenhum turno nesta janela." />
```

Name the slice ("nesta janela", "neste projeto"). A bare "Nenhum dado" leaves the user unsure whether the filter or the collection is at fault.
